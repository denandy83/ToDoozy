.env
node_modules/
dist/
out/
*.tsbuildinfo
*.db
*.db-journal
.DS_Store
.claude/worktrees/

# Supabase backup (PII + auth secrets — never commit)
backup-supabase-*/
.env.bak*

# Local DB backups — personal data, never commit/push
backups/
