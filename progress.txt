# Ralph Progress Log
Started: Fri May  1 07:48:09 CDT 2026
---

---

## Story #65 — Profile Settings (2026-05-01)

**Implemented:**
- Part A: Added `profile` as first tab in `UnifiedSettingsModal.tsx`. Default tab is now `profile`.
- Part B: Created `ProfileSettingsContent.tsx` — Account (email read-only), Display Name (first/last autosave to SQLite + Supabase auth metadata, offline detection), Password (Change/Add with validation, eye toggles, explicit save).
- Part C: Updated `LoginScreen.tsx` — added `forgot` mode with forgot password flow. Main form/divider/Google/toggle hidden in forgot mode. `handleForgotPassword` calls `send-password-reset` Edge Function.
- Part D: Created `supabase/functions/send-password-reset/index.ts` — differentiates email/password users (recovery link → reset-password.html) from Google-only users (magic link → no-password.html). Deployed to Supabase.
- Part E: Created `docs/reset-password.html` (set new password in browser, same validation, Supabase JS CDN) and `docs/no-password.html` (static info page for Google users).
- Part F: Added profile sync retry in both `onRecovered` callbacks in `authStore.ts` — re-pushes `display_name` to Supabase auth metadata when `profile_sync_pending` setting is `'true'`.

**Decisions:**
- Removed unused `useEffect` import in `LoginScreen.tsx` (that's for story #66's credential pre-fill).
- `verify_jwt: false` on the Edge Function — it's intentionally unauthenticated (same as Supabase's password reset workflow).
- No new repository methods → no new Vitest tests needed. All 741 existing tests pass.

---

## Story #66 — Save Login Credentials (2026-05-01)

**Implemented:**
- Installed `keytar` + `@types/keytar` (native OS Keychain / Credential Manager wrapper)
- Added `getSavedEmailPath()` helper and `KEYTAR_SERVICE = 'ToDoozy'` constant in `ipc-handlers.ts`
- Added 5 IPC handlers: `auth:saveEmail`, `auth:getSavedEmail`, `auth:savePassword`, `auth:getSavedPassword`, `auth:clearCredentials`
- Added 5 preload bridge methods and updated `AuthAPI` interface in `index.d.ts`
- `authStore.signInWithEmail` now returns `Promise<boolean>`, saves email + sets `pendingPasswordSave` on success
- `authStore.signUpWithEmail` saves email on success (no password prompt)
- `authStore.signInWithGoogle` saves email (both implicit + PKCE branches) on success
- `authStore.logout` calls `clearCredentials` to delete both saved-email JSON and Keychain entry
- Exported `takePendingPasswordSave()` module-level function from authStore
- `AppLayout.tsx` calls `takePendingPasswordSave()` on mount; if pending, shows "Save password to Keychain?" persistent toast with Save/No thanks actions
- `LoginScreen.tsx` `useEffect` pre-fills email and password from saved JSON/Keychain on mount (cancelled on cleanup)

**Decisions:**
- `signInWithEmail` return type changed from `Promise<void>` to `Promise<boolean>` — updated interface too
- No changes to tests (no new repository methods)
