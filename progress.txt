# Ralph Progress Log
Started: Sun Mar 22 10:02:36 CET 2026
---

## Story #30: Global Labels — Shared Across All Projects
**Completed:** 2026-03-22

### What was done:
- **Migration 6**: Created `project_labels` junction table, populated from existing data, merged duplicate label names (oldest wins), recreated `labels` table without `project_id` FK, recreated `task_labels` with FK to new table
- **LabelRepository**: Full rewrite — `findAll()`, `findByName()`, `addToProject()`, `removeFromProject()`, `findAllWithUsage()`, `findProjectsUsingLabel()`, `findActiveLabelsForProject()`
- **Types**: Removed `project_id` from `Label` interface, added `ProjectLabel` and `LabelUsageInfo` types, made `project_id` optional in `CreateLabelInput`
- **IPC/Preload**: Added 6 new IPC handlers for the new repository methods
- **Label Store**: Added `projectLabels` state (projectId -> Set<labelId>), `hydrateAllLabels()`, `addToProject()`, `removeFromProject()` actions
- **useCreateOrMatchLabel hook**: Shared logic for label creation — checks for existing global label by name, adds to project with toast if found, creates new if not
- **LabelPicker consumers**: Updated DetailPanel, TaskRow, TaskListView, ContextMenu, BulkContextMenu, MyDayView to use `useCreateOrMatchLabel`
- **LabelSettingsContent**: Full rewrite — shows all global labels with project/task usage counts, edit name/color globally, "Remove from project" and "Delete everywhere" flows with red destructive styling and confirmation toasts
- **LabelFilterBar**: Already filters to labels assigned to non-archived tasks (no change needed)
- **MCP Server**: Updated `create_label`, `use_task_template`, `deploy_template`, and `copyTemplateSubtasks` to use global label matching
- **Tests**: Updated existing tests to remove `project_id` from Label, added 12 new LabelRepository tests

### Decisions:
- Kept `project_id` as optional in `CreateLabelInput` for backward compat — if provided, auto-links to that project
- Label settings shows ALL global labels (not just project-scoped) with usage counts
- The `useCreateOrMatchLabel` hook centralizes the "create or match" logic to avoid duplication across 6+ components
- Pre-existing test failures (shortcut-utils, smartInputParser) are unrelated; repository tests fail due to Node.js native module version mismatch (not our issue)

## Story #31: Fix Tab Order and Focus Management

### What was done:
- Created `useFocusTrap` hook: traps Tab/Shift+Tab within a container, cycles between first and last focusable elements
- Created `useFocusRestore` hook: saves active element on mount, restores on unmount
- Applied focus trap + restore to: Modal, CommandPalette, ContextMenu, BulkContextMenu, LabelPicker, DeployProjectTemplateWizard
- Sidebar: added `tabIndex={-1}` to aside, all nav items, buttons, and inputs — sidebar is now unreachable via Tab
- Task list: Enter/Space now opens detail panel and focuses title (replaces old move-mode and status-cycle behavior)
- Task list: Tab stopPropagation prevents AppLayout from intercepting Tab when inside task list
- Task list: auto-focuses container when navigating to a project
- Detail panel: Tab cycles through fields (title→status→priority→labels→due date→description) using `data-detail-field` attributes
- Detail panel: Escape closes panel and restores focus to the previously focused element (task row)
- TimerPlayButton: changed tabIndex from -1 to 0
- Add-label button in TaskRow: changed tabIndex from -1 to 0, added focus-visible ring
- Global CSS: added `*:focus-visible` style with accent ring for consistent keyboard focus indicator

### Decisions:
- Enter/Space in task list changed from move-mode/status-cycle to opening detail panel (per story requirements). Move mode is still accessible but needs a new trigger.
- Used `data-detail-field` attributes instead of refs for Tab cycling — simpler, works across column layouts
- Focus-visible ring uses CSS `box-shadow` instead of Tailwind ring utility to apply globally without class changes
