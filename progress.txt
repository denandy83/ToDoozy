# Ralph Progress Log
Started: Wed Mar 18 10:59:51 CET 2026
---

## Story 1: Scaffold Electron + React + TypeScript project
- Scaffolded manually (electron-vite create is interactive, so set up structure by hand)
- Installed all deps: better-sqlite3, @dnd-kit/core, @dnd-kit/sortable, lucide-react, tailwindcss@4, @tailwindcss/vite@4, zustand, @modelcontextprotocol/sdk, @supabase/supabase-js
- Configured TypeScript strict mode (tsconfig.node.json + tsconfig.web.json)
- Set up Tailwind CSS 4 via @tailwindcss/vite plugin with theme colors
- Dev server configured on port 5200
- Copied ToDoozy.png to resources/icon.png
- electron-vite v5 requires vite@^7 (not v8) — pinned accordingly
- Preload uses globalThis cast instead of window to avoid DOM type issues in Node tsconfig
- Typecheck passes, dev server compiles and launches successfully

## Story 2: Database layer with versioned migrations
- Created src/main/database/ module with index.ts, migrations.ts, seed.ts
- All 10 tables from §4 Data Model: users, projects, project_members, statuses, tasks, labels, task_labels, themes, settings, activity_log
- schema_version table tracks migration versions; migrations run in transactions
- PRAGMA foreign_keys = ON, WAL mode enabled
- Seeded 12 built-in themes (6 dark, 6 light) and default settings
- Default statuses (Not Started, In Progress, Done) are seeded per-project at project creation time (Story 7), not globally
- Ran @electron/rebuild to fix better-sqlite3 native module for Electron's Node version
- Database initializes on app.whenReady(), closes on app.will-quit
- Typecheck passes, dev server compiles and migration runs successfully

## Story 3: Repository pattern
- Created src/shared/types.ts with all domain types (User, Project, Status, Task, Label, Theme, Setting, ActivityLogEntry) plus create/update input types
- TASK_UPDATABLE_COLUMNS whitelist for SQL injection prevention on task updates
- Created 8 repository classes in src/main/repositories/:
  - UserRepository: CRUD, findByEmail
  - ProjectRepository: CRUD, findByOwnerId, findDefault, member management (add/remove/get), getProjectsForUser
  - StatusRepository: CRUD, findByProjectId, findDefault, findDone, reassignAndDelete (transactional)
  - TaskRepository: CRUD, findByProjectId/StatusId/MyDay/Archived/Templates, findSubtasks, getSubtaskCount, reorder (batch transaction), label assignments, duplicate
  - LabelRepository: CRUD, findByProjectId, findByTaskId
  - ThemeRepository: CRUD, listByMode, getConfig (parsed JSON)
  - SettingsRepository: get/set/getAll/getMultiple/setMultiple (transactional), delete
  - ActivityLogRepository: create, findByTaskId/UserId, deleteByTaskId, getRecent
- createRepositories() factory function returns all repos from a single db instance
- Updated tsconfig.node.json and tsconfig.web.json to include src/shared/**/*.ts
- 54 Vitest unit tests covering all repository methods — all pass
- Typecheck passes, dev server compiles successfully

## Story 4: IPC layer and preload bridge
- Created src/main/ipc-handlers.ts with registerIpcHandlers() mapping every repository method to an ipcMain.handle() channel
- Channel naming convention: `namespace:method` (e.g., `tasks:findById`, `labels:create`)
- Lazy initialization of repositories via getRepos() — repos created on first IPC call after DB is ready
- Updated src/preload/index.ts: fully typed api object exposed via contextBridge with all 8 namespaces (tasks, labels, projects, statuses, users, activityLog, settings, themes)
- Updated src/preload/index.d.ts: complete type declarations for TodoozyAPI with separate interfaces per namespace
- Return values: undefined from repos converted to null for IPC serialization (undefined not serializable)
- All methods return Promises in the preload API (ipcRenderer.invoke is async)
- Registered IPC handlers in main/index.ts after initDatabase()
- Typecheck passes with zero errors, dev server compiles successfully

## Story 5: Zustand Stores
- Created 6 Zustand stores: taskStore, labelStore, statusStore, projectStore, settingsStore, authStore
- Each store follows hydration pattern: load from IPC, store in Record<id, Entity> maps
- All actions wrap IPC calls with try/catch, storing error messages in state
- Exported typed selectors for common queries (by project, by status, current selection, etc.)
- LabelStore includes activeLabelFilters (Set<string>) for filter UI
- ProjectStore auto-selects default project on hydration if none selected
- SettingsStore combines settings + themes management, restores currentThemeId from settings
- AuthStore manages user session state with login/logout/checkAuth
- Barrel export from shared/stores/index.ts
- Zero any types, all interfaces defined, typecheck passes clean

## Story 6: Supabase Auth - Login screen
- Created auth IPC handlers in ipc-handlers.ts: safeStorage-based session persistence (storeSession, getSession, clearSession), getSupabaseConfig, openOAuthWindow
- safeStorage encrypts session JSON using OS keychain; falls back to plaintext if encryption unavailable
- OAuth flow: main process opens BrowserWindow popup, watches navigation events for callback URL with code/access_token
- Added dotenv to main process entry (src/main/index.ts) to load .env credentials
- Extended preload bridge with AuthAPI interface (5 methods) and type declarations in index.d.ts
- Created src/renderer/src/lib/supabase.ts: lazy-initialized Supabase client with config from IPC, plus URL token parser
- Rewrote authStore with full Supabase integration:
  - signInWithEmail / signUpWithEmail: direct Supabase auth calls
  - signInWithGoogle: OAuth popup flow via IPC → BrowserWindow → token extraction → session set
  - initAuth: restores session from safeStorage on launch, auto-refreshes via Supabase, offline fallback using local SQLite users
  - ensureDefaultProject: creates "Personal" project + 3 default statuses on first login
  - persistSession: stores access_token + refresh_token to safeStorage after every auth event
- Created LoginScreen component (src/renderer/src/features/auth/LoginScreen.tsx):
  - Email/password form with login/signup toggle
  - Google OAuth button with SVG icon
  - Loading spinner, error display, keyboard navigation (Enter to submit, Escape clears error)
  - Typography and styling follow spec (§35 UX Consistency, typography scale)
- Updated App.tsx: auth routing — splash screen during init, LoginScreen when unauthenticated, main app when authenticated
- Rebuilt better-sqlite3 native module for Electron with @electron/rebuild
- Typecheck passes with zero errors, dev server compiles and launches successfully

## Story 7: Projects and configurable statuses
- Built shared Modal and Toast components (src/renderer/src/shared/components/)
  - Modal: backdrop blur, escape to close, click-outside to close, zoom-in animation
  - Toast: global state via useToast() hook, auto-dismiss 5s, action buttons, stacking, variant support
- Built ProjectSwitcher dropdown (src/renderer/src/features/projects/ProjectSwitcher.tsx)
  - Lists all user projects with color indicators
  - Click to switch active project (updates projectStore.currentProjectId)
  - Settings gear button per project, "New Project" button at bottom
  - Click-outside and Escape to close
- Built NewProjectModal: name + color picker, seeds 3 default statuses on create
- Built ProjectSettingsModal with tabbed layout (General / Statuses)
  - General tab: autosave name (1s debounce), color picker, danger zone with delete confirmation
  - Default project cannot be deleted
  - Statuses tab: full CRUD with StatusList, StatusEditRow, ReassignStatusModal
- Status CRUD:
  - Add: name, color picker (8 presets), is_done toggle
  - Edit inline: same fields, Escape to cancel
  - Delete: ReassignStatusModal prompts user to reassign tasks to another status before deletion
  - Cannot delete last status
- Built AppLayout with sidebar: project switcher header, nav placeholder, settings/logout footer
- App.tsx hydrates projectStore and statusStore on auth, re-hydrates statuses on project switch
- Typecheck passes with zero errors, production build succeeds
