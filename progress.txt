# Ralph Progress Log
Started: Sat Apr 11 22:32:43 CEST 2026
---

## Story #59: Remote MCP Server via Supabase Edge Function
- Created `api_keys` table migration (003) with RLS and migration from user_settings
- Created Supabase Edge Function at `supabase/functions/mcp/index.ts` with Streamable HTTP transport
  - All 45 tool handlers ported from local mcp-server.ts
  - API key authentication via `api_keys` table with request_count tracking
  - CORS headers, session management, proper error responses (401/400)
  - Repos inlined for Deno compatibility (npm: specifiers for @supabase/supabase-js and @modelcontextprotocol/sdk)
  - Recurrence utils inlined to avoid cross-module Deno import issues
- Removed local stdio MCP server entirely:
  - Deleted `src/main/mcp-server.ts`
  - Removed `mcp-server` from rollupOptions in electron.vite.config.ts
  - Removed `writeMcpSession`, `deleteMcpSession`, `getMcpSessionPath` from ipc-handlers.ts
  - Removed `mcp:getInfo` and `mcp:isRunning` IPC handlers
  - Removed `McpAPI` interface and `mcp` property from preload bridge
  - Removed `McpIndicator` component from sidebar
  - Removed `execSync` import (no longer needed)
- Restructured Settings > Integrations UI:
  - API key section moved above sub-tabs (shared across all integrations)
  - API key handlers updated to write to `api_keys` table instead of `user_settings`
  - MCP tab rewritten with setup instructions for Claude Code, Gemini CLI, Codex CLI, and JSON config
  - All setup commands auto-fill user's API key
  - MCP on/off toggle removed, no ELECTRON_RUN_AS_NODE references
  - iOS Shortcut tab references shared API key above
- Updated `.mcp.json` with ToDoozy remote entry (manual update needed — file is sensitive)
- Created migration 004 updating `quick_add_task` function to use `api_keys` table with backward compatibility
- Decision: deploy_project_template handler throws "not available via remote" since ProjectTemplateRepository is local-only
- Note: `.mcp.json` update was denied by permission system — user needs to add ToDoozy entry manually
